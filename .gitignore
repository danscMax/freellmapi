node_modules/
dist/
server/data/
*.db
*.db-wal
*.db-shm
*.bak
.env
.env.local
.DS_Store

# Personal deployment scripts (contain keys/credentials — kept local)
deploy-pi.sh
update-hermes.sh

# Monetization planning — private, never committed
monetization/

# Empty placeholder artifact dropped by tooling
Claude

# Local machine-specific launcher scripts (tied to a local schtask + ports)
tools/
